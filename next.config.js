/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    loader: 'default',
    domains: ["tim-charteris-portfolio.netlify.app", "localhost"],
  },
}

module.exports = nextConfig
